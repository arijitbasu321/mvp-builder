{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Task",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'GOLDEN RULES — Read before proceeding:\n1. Agent Teams with worktrees — All teammates are spawned via TeamCreate/Task with team_name. No bare sub-agents.\n2. Unit tests with coverage — All code must have unit tests. Coverage must meet the project threshold (default 80%). All tests must pass.\n3. Playwright E2E tests — UI features must have Playwright tests validating user flows.\n4. LEARNINGS.md is maintained — Every discovery, gotcha, or pattern that affects future work is recorded.\n5. DECISIONS.md is append-only — Every settled decision is logged. Never edited or deleted.\n6. ISSUES.md is PM-only — Only the Project Manager writes to ISSUES.md. Teammates report issues via SendMessage.\n7. STATE.md stays small — STATE.md must not exceed 50 lines.'"
          }
        ]
      }
    ]
  }
}
